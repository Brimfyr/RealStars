# Build output
bin/
obj/
dist/

# Local StarMap launcher config, not part of the mod
StarMapConfig.json

# Catalogue sources and built binaries are deliberately not in the repository: they are
# large, and the shipped star binary is generated rather than authored. See release-files.txt.
assets/
